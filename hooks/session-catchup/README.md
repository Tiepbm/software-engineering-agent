# Session Catchup

`sessionStart` hook that summarizes active `.handoffs/<ADR>/` state and uncommitted diff stats so the next agent turn can recover context quickly.

## Environment

- `SKIP_SESSION_CATCHUP=true`

